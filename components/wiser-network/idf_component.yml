version: "0.1.0"
description: "Elecbits ESP-IDF component providing WiFi station connectivity management and BLE-based WiFi/cloud credential provisioning."
dependencies:
  idf: ">=5.0"

# TODO: repository: https://github.com/<elecbits-org>/eb_network.git
# TODO: url: https://github.com/<elecbits-org>/eb_network
# TODO: issues: https://github.com/<elecbits-org>/eb_network/issues
#
# NOTE: This component depends on the sibling "eb_storage" component
# (NVS storage layer) at build time (see CMakeLists.txt PRIV_REQUIRES).
# eb_storage is not published yet, so it is intentionally NOT listed as
# a dependency here. Consuming projects must make eb_storage available
# locally alongside this component — see README.md.
